# Environment Variables per Production
# Copia questi valori su Vercel → Settings → Environment Variables

# =============================================================================
# DATABASE - PostgreSQL Neon
# =============================================================================
# Usa la POOLED connection string da Neon dashboard
# Deve includere: ?sslmode=require&pgbouncer=true
DATABASE_URL="postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/domusreport?sslmode=require&pgbouncer=true"

# =============================================================================
# AUTHENTICATION - NextAuth.js
# =============================================================================
# Genera con: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-jwt-key-min-32-chars"

# URL production app
NEXTAUTH_URL="https://domusreport.mainstream.agency"

# =============================================================================
# OPENAI API
# =============================================================================
# Chiave API OpenAI per GPT-4
# Verifica crediti e rate limits prima del deploy
OPENAI_API_KEY="sk-proj-..."

# =============================================================================
# GOOGLE MAPS API
# =============================================================================
# Per geocoding indirizzi
# Abilita "Geocoding API" su Google Cloud Console
GOOGLE_MAPS_API_KEY="AIza..."

# =============================================================================
# N8N WEBHOOK (Temporaneo)
# =============================================================================
# Per ora usa placeholder, aggiornerai dopo aver configurato n8n
N8N_WEBHOOK_URL="https://placeholder.com/webhook"

# =============================================================================
# APP URLS (Public)
# =============================================================================
# URL base applicazione (per widget, redirect, etc)
NEXT_PUBLIC_APP_URL="https://domusreport.mainstream.agency"

# URL CDN per widget embed (per ora stesso dell'app)
NEXT_PUBLIC_WIDGET_CDN_URL="https://domusreport.mainstream.agency"

# =============================================================================
# MONITORING (Opzionale)
# =============================================================================
# Sentry DSN per error tracking
# SENTRY_DSN="https://...@o...ingest.sentry.io/..."
# SENTRY_AUTH_TOKEN="..."
# SENTRY_ORG="..."
# SENTRY_PROJECT="domus-report"

# =============================================================================
# PREVIEW ENVIRONMENTS (Vercel)
# =============================================================================
# Per branch preview su Vercel, usa variabili dinamiche:
# NEXTAUTH_URL="https://${VERCEL_URL}"
# NEXT_PUBLIC_APP_URL="https://${VERCEL_URL}"

# =============================================================================
# DEVELOPMENT
# =============================================================================
# Per local development con production DB (non raccomandato):
# DATABASE_URL="[production-url]"
# NEXTAUTH_URL="http://localhost:3000"
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
